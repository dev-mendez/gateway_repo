# Gateway app



## Prerequisites to have installed

These are requirements to run the app in local machine. 

You will need to install the following:

- Nodejs
  https://nodejs.org/en/download/
  
- Mongodb
  https://docs.mongodb.com/manual/installation/
  ```


Install requirements
Use npm install on the root of both folder, gateway_ui and gateway_server to install dependencies.
|
|
|
|
|
Run the server first in htttp://localhost:3000 see package.json for more information and scripts to run the server.
|
|
|
|
|
Run the client. For this time the client will need a server conection to localhost:3000. See package.json for more information and scripts to run the client.




